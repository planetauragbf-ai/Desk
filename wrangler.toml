# Configuration Cloudflare Pages (site statique)
# Build : `npm run build` → dist/ ; déploiement : `npx wrangler pages deploy`.
# URL de l'application : https://planet-desk.pages.dev
name = "planet-desk"
compatibility_date = "2026-07-01"
pages_build_output_dir = "dist"
